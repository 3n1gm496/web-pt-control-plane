version: 1
name: production
description: >
  Explicit allowlist for passive production checks. Replace the example hosts
  with domains you own before exposing the API.
hosts:
  - www.camera.it
  - servizi.camera.it  
